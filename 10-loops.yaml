- name: loops
  hosts: local 
  connection: local
  tasks:

  - name: print the course list using loop
    ansible.builtin.debug:
      msg: "{{ item }}"
    loop:
    - linux
    - shell  
    - ansible
    
  